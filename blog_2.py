from flask import Flask, render_template

app = Flask(__name__)

@app.route('/')   #这里默认只接受get，get就是想服务器索取信息的意思，后续会学习使用post命令，是浏览器告诉服务器我要交东西
#这些命令实际是浏览器和服务器之间的沟通语言
def index():
    posts = [
        {
            'id': 1,   
            'title': 'Flask 入门教程',
            'date': '2026-07-22',
            'summary': 'Flask 是一个轻量级的 Python Web 框架，几行代码就能跑起一个网站。'
        },
        {
            'id': 2,   
            'title': 'CSS Flexbox 布局详解',
            'date': '2026-07-21',
            'summary': 'Flexbox 是 CSS 的弹性盒子布局模型，是现代网页排版的必备技能。'
        }
    ]
    return render_template('content.html', posts=posts)

@app.route('/post/<int:post_id>')    #这里是当收到此地址时输出的反应，这里链接到了阅读文章详情页的部分，我们给文章进行了排号，在html文件中也做了循环，相当于此处是用户点击阅读全文后应该给的反应
def view_post(post_id):   #定义了一个对应的反应结果函数，输入的变量是post_id
    posts_2 = {   #定义了一个临时的字典用作数据库的作用，可以输出id对应的内容
        1: {'title': 'Flask 入门', 'content': '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'
        '首页负责显示文章列表，用户看到所有文章的标题和摘要。每篇文章底部有一个“阅读全文”链接，链接指向 /post/{{ post.id }}。详情页只显示一篇文章的完整内容。首页用 {% for %} 循环遍历所有文章，详情页直接取单篇文章，不需要循环。首页接收的数据是一个列表，详情页接收的数据是一个字典。两者的数据结构不同，模板里的写法也不同。'},
        2: {'title': 'CSS 教程', 'content': 'CSS 用于控制网页样式'}
    }
    
    post = posts_2.get(post_id)  #从我们定义的字典中利用id，也就是键名取获取对应的值，存储到post这个变量中
    #这里也可以使用posts_2[post_id]输出值，但get函数会更安全
    #这里的post在不同的路由以及函数中类似局部变量，与上面的post是不同的值
    if post:   #如果post有值，就使用如下模板文件和数据
        return render_template('post.html', post=post)
    else:
        return '<h1>文章不存在</h1><a href="/">返回首页</a>', 404

if __name__ == '__main__':
    app.run(debug=True)